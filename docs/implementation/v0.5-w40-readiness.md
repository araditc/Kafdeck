# v0.5 W40 Readiness Record

- **Authority:** Issue #121 / RFC-0005
- **Tracker:** Issue #122
- **Workstream:** Issue #140
- **Baseline protected main:** `f8e6b702d54bf68edffb3e672dec282f0b499c87`
- **Status:** ACTIVE — release readiness only
- **Release authority:** NOT GRANTED

## Purpose

W40 closes the v0.5 implementation cycle by collecting security, compatibility,
persistence/recovery, fault-injection, bounded-load and operator-readiness evidence.
It may prepare a release-candidate manifest proposal, but it must not create a tag,
promote an OCI image, publish a GitHub Release, or perform any other publication
action without explicit owner approval.

## Inherited release blocker

PR #139 was merged to protected `main` as
`f8e6b702d54bf68edffb3e672dec282f0b499c87`.

Its exact source head
`0fb0e2353b7474305a2a511b1918ec5327b51cb3`
had green language-level CodeQL jobs but the GitHub Advanced Security aggregate
CodeQL protection check `107045429979` reported **one new High-severity alert**.

That alert was not dismissed or suppressed. It remains a mandatory W40 release
blocker until the exact CodeQL finding is identified, remediated, and fresh
security evidence is green.

The current connector does not expose the check-run annotation subresource and
the CodeQL workflow retained no SARIF artifact. Therefore W40 must treat the
finding as unresolved security debt rather than infer or waive it.

## Post-merge baseline evidence

At protected `main@f8e6b702d54bf68edffb3e672dec282f0b499c87`:

- `quality-gate` — SUCCESS after one evidence-based rerun of a timing-sensitive,
  pre-existing stale-cache test;
- `codeql-csharp` — SUCCESS;
- `codeql-javascript-typescript` — SUCCESS.

The initial post-merge quality-gate failure was limited to
`ClusterExplorerServiceTests.Retryable_refresh_failure_serves_stale_metadata_as_degraded`
(Expected `Degraded`, observed `Unavailable`). The identical source tree had
passed immediately before merge, so only that failed job was rerun; no source,
test, timeout, or governance rule was weakened.

## Readiness evidence required

### Security

- identify and remediate the inherited GHAS High alert;
- reconcile the v0.5 threat model against all W32-W39 mutation surfaces;
- verify no broad `admin` bypass;
- verify read permission never implies mutation permission;
- verify current-request authorization immediately before dispatch;
- verify CRITICAL independent approval remains enforced;
- verify no durable raw Kafka payload/key/header or provider secret staging;
- verify no generic Kafka/AdminClient/CLI/provider command surface;
- verify no arbitrary server-side code execution;
- verify no blind retry after potentially applied dispatch;
- preserve explicit `ExecutionUnknown` semantics.

### Compatibility

- Kafka 4.3.1;
- Kafka 4.2.1;
- Kafka 4.1.2;
- Kafka 3.9.2;
- Schema Registry mutation fixture evidence;
- Kafka Connect mutation fixture evidence.

### Persistence and recovery

- SQLite standalone recovery evidence;
- PostgreSQL HA concurrency/claim/idempotency evidence;
- interrupted execution recovery;
- stale claim/resource-claim reconciliation;
- backup/restore implications for mutation state and HMAC-bound material metadata.

### Fault injection

- timeout after possible provider dispatch;
- lost provider response;
- partial provider result;
- verification/readback inconclusive;
- stale-preview drift immediately before dispatch;
- executor/process interruption around claim and dispatch boundaries.

### Performance and boundedness

- v0.3 bounded-processing regression evidence;
- v0.4 read-view regression evidence;
- bounded mutation concurrency;
- request/material byte ceilings;
- bounded approval inbox and target materialization;
- no unbounded provider response or durable evidence growth.

### Operator readiness

- operator guide;
- upgrade/migration guide;
- backup/restore notes;
- `ExecutionUnknown` handling guidance;
- destructive/CRITICAL operation guidance;
- independent approval workflow guidance.

## Final admission gate

W40 may be declared readiness-complete only when:

1. the inherited GHAS High alert is resolved by code/evidence, not suppression;
2. all applicable exact-head CI and security checks are green;
3. all W40 evidence listed above is recorded and reviewable;
4. zero substantive review threads remain unresolved;
5. fresh CODEOWNER approval exists on the final exact head;
6. protected `main`, ruleset and mergeability are reconciled;
7. no release/tag/publication action has occurred.

After W40 merge, stop for explicit owner release approval before any v0.5 tag,
OCI promotion, GitHub Release or publication.
