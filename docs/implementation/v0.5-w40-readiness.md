# v0.5 W40 Readiness Record

- **Authority:** Issue #121 / RFC-0005
- **Tracker:** Issue #122
- **Workstream:** Issue #140
- **Baseline protected main:** `f8e6b702d54bf68edffb3e672dec282f0b499c87`
- **Canonical branch:** `feat/v0.5-w40-readiness`
- **Status:** ACTIVE — candidate evidence assembled; final exact-head admission pending
- **Release authority:** NOT GRANTED

## Purpose

W40 closes the v0.5 implementation cycle by collecting security, compatibility,
persistence/recovery, fault-injection, bounded-load and operator-readiness evidence.
It may prepare a release-candidate manifest proposal, but it must not create a tag,
promote an OCI image, publish a GitHub Release, or perform any other publication
action without explicit owner approval.

## Exact-head evidence rule

Any source-head change invalidates prior exact-head CI and CODEOWNER review evidence
for final admission. Historical evidence may establish that a control or workflow
has worked, but W40 may be merged only after all applicable checks are green on the
final exact source head, all substantive review threads are resolved, and a fresh
CODEOWNER approval exists on that head.

## Inherited security finding

PR #139 was merged to protected `main` as
`f8e6b702d54bf68edffb3e672dec282f0b499c87`.
Its exact source head
`0fb0e2353b7474305a2a511b1918ec5327b51cb3`
produced one new High-severity GitHub Advanced Security aggregate finding:

- **CodeQL rule:** `cs/insufficient-password-hash`
- **CWE:** CWE-916
- **Path:** `tests/Kafdeck.Architecture.Tests/V05ConnectSecretPreviewTests.cs`

The alert was never dismissed or suppressed. Commit
`44858ef8597385c3650b62b43b1442cef29aa289` removed the raw-password SHA flow in
the test fixture. Fake provider observations now use fixed precomputed raw
fingerprint fixtures, while the keyed-fingerprint assertion compares the same
sensitive values under independent governed `IMutationMaterialDigestService`
keys.

On W40 source head `32517e3749847eeb1dd54449e3fd243a755425bb`, fresh
CodeQL C# and JavaScript/TypeScript jobs succeeded and GHAS aggregate check
`107081671681` completed **SUCCESS** with **No new alerts in code changed by this
pull request** and zero annotations. Because this readiness record and supporting
documentation subsequently changed the source head, fresh final-head CodeQL/GHAS
evidence remains mandatory before admission.

## Pre-reconciliation exact-head automated baseline

On `32517e3749847eeb1dd54449e3fd243a755425bb` all applicable automated evidence
was green:

- `quality-gate` — SUCCESS (`35830399749`);
- `dependency-review` — SUCCESS (`35830399564`);
- CodeQL C# + JavaScript/TypeScript — SUCCESS (`35830399720`);
- GHAS aggregate CodeQL — SUCCESS (`107081671681`), no new alerts / zero annotations;
- `kafka-smoke` — SUCCESS (`35830399594`);
- Kafka compatibility — SUCCESS (`35830399572`): 4.3.1, 4.2.1, 4.1.2 and 3.9.2;
- `v05-persistence` — SUCCESS (`35830399573`), PostgreSQL 18.6 fixture, 53/53
  mutation-kernel/persistence tests passed;
- v0.3 benchmark — SUCCESS (`35830399861`);
- v0.4 benchmark — SUCCESS (`35830399816`).

This proves the relevant workflows and controls on that exact source tree. It does
not substitute for the final-head gate after the documentation/reconciliation
changes in this PR.

## Readiness evidence

### Security and threat model

- [x] W32-W39 threat-model reconciliation recorded in
  `docs/security/v0.5-w40-threat-model-reconciliation.md`;
- [x] inherited GHAS High finding remediated without suppression and independently
  verified on exact head `32517e...`; final-head GHAS revalidation remains part of
  the admission gate below;
- [x] no broad `admin` bypass: mutation authorization is expressed through typed
  actions and the authorization loader/kernel tests exercise explicit actions;
- [x] read permission never implies mutation permission: explicit mutation action
  evaluation is independently tested;
- [x] current-request authorization is re-evaluated immediately before dispatch;
  `V05MutationRequestAuthorizationTests` and `V05W39PreDispatchGuardTests` fail
  closed for missing, changed or different current request identities;
- [x] CRITICAL independent approval remains enforced: requester self-approval is
  rejected and the second principal must independently satisfy the exact typed
  authorization target;
- [x] raw Kafka key/value/header material is not durably staged by default;
  record-production tests prove raw sentinels are absent from the canonical plan
  and persisted operation snapshot and re-submitted material is HMAC-digest bound;
- [x] provider secrets are excluded from durable mutation/audit/evidence contracts;
  provider evidence is bounded and rejects credential-like keys/values;
- [x] no generic Kafka/AdminClient/CLI/provider command surface: mutation ports and
  provider adapters expose fixed typed operations/routes;
- [x] no arbitrary server-side JavaScript/SQL/shell/runtime-code execution surface;
- [x] no blind retry after potentially applied dispatch;
- [x] ambiguous post-dispatch outcomes remain explicit `ExecutionUnknown` until
  bounded verification/reconciliation proves a stronger result;
- [x] OIDC/session state-changing mutation endpoints retain ASP.NET anti-forgery
  protection and strict HTTPS cookie posture.

### Compatibility

- [x] Kafka 4.3.1 — compatibility lane passed on exact head `32517e...`;
- [x] Kafka 4.2.1 — compatibility lane passed on exact head `32517e...`;
- [x] Kafka 4.1.2 — compatibility lane passed on exact head `32517e...`;
- [x] Kafka 3.9.2 — compatibility lane passed on exact head `32517e...`;
- [x] Schema Registry mutation fixture evidence: `SchemaMutationAdapterTests`
  proves fixed typed Confluent-compatible routes, redirect/auth fail-closed
  behavior, bounded/safe provider evidence, definitive conflicts and conservative
  `ExecutionUnknown` mapping for ambiguous post-dispatch server failures;
- [x] Kafka Connect mutation fixture evidence: `KafkaConnectMutationAdapterTests`
  proves fixed typed routes, admitted-profile enforcement, redirect rejection,
  bounded response handling, private-provider-text suppression and conservative
  timeout/rate-limit/server-failure outcome mapping.

Fresh final-head workflow execution is still required where the repository workflow
is applicable; fixture evidence here describes the tested contract, not a generic
external-provider certification.

### Persistence and recovery

The `v05-persistence` lane on `32517e...` ran against PostgreSQL 18.6 and passed
53/53 filtered W32 persistence/kernel tests.

- [x] SQLite standalone idempotency/CAS/resource-claim evidence;
- [x] SQLite per-cluster execution-slot cap evidence;
- [x] SQLite HA mutation posture fails closed rather than pretending to coordinate
  multiple instances;
- [x] PostgreSQL cross-repository idempotency/CAS/resource-claim evidence;
- [x] PostgreSQL global per-cluster concurrency and atomic lease-renewal evidence;
- [x] PostgreSQL claim/recovery race leaves no orphan resource claim;
- [x] interrupted pre-dispatch execution is recovered as definitive
  `FailedBeforeDispatch`;
- [x] interrupted post-dispatch execution is recovered conservatively as
  `ExecutionUnknown`;
- [x] active leases and live cluster slots are preserved during recovery;
- [x] unknown execution retains its conflict/resource claim as a safety control;
- [x] ephemeral execution material must digest-match before dispatch and is not
  persisted;
- [x] backup/restore, HMAC material-digest secret and unknown-operation recovery
  implications are documented in `docs/operator/v0.5-safe-administration-guide.md`
  and `docs/operator/v0.5-upgrade-guide.md`.

### Fault injection and ambiguous outcomes

- [x] provider timeout after dispatch can no longer be represented as a safe
  pre-dispatch failure; it remains `ExecutionUnknown`;
- [x] provider work ignoring cancellation is bounded by the executor timeout and
  retains the conflict/cluster slot until late work completes;
- [x] null and malformed provider results are durably classified as
  `ExecutionUnknown`;
- [x] caller cancellation after dispatch does not cancel server-owned provider
  execution;
- [x] record-production partial acknowledgement is represented as
  `PartiallyApplied`, with known acknowledgements retained;
- [x] purge verification that proves only part of the post-condition remains
  `AppliedUnverified` rather than falsely claiming full success;
- [x] stale-preview drift is detected immediately before dispatch for typed
  mutation surfaces, including record-production topic drift and purge low-watermark
  drift;
- [x] process interruption around claim/dispatch is classified using the durable
  dispatch boundary rather than by blind retry semantics;
- [x] post-dispatch audit failure does not erase a known provider outcome or cause
  duplicate dispatch.

### Performance and boundedness

- [x] v0.3 bounded-processing regression benchmark passed on exact head
  `32517e...` (`35830399861`);
- [x] v0.4 read-view regression benchmark passed on exact head `32517e...`
  (`35830399816`);
- [x] mutation execution has a configured per-cluster concurrency bound; SQLite
  and PostgreSQL evidence verifies the bound under their admitted deployment
  modes;
- [x] mutation intent collections are hard bounded;
- [x] governed record production enforces per-record and aggregate key/value/header
  and execution-material ceilings before preview;
- [x] destructive target materialization is finite and bounded; purge planning
  rejects an over-bound target set and mutation intent resource/authorization
  collections are bounded by the kernel;
- [x] approval inbox is bounded: the API defaults to 50 visible items, rejects
  requested limits outside 1..100, and caps authorization scanning at 10,001
  candidates;
- [x] provider response/evidence growth is bounded: Schema Registry/Connect adapters
  bound response material and durable `MutationProviderEvidence` enforces bounded
  keys/values while rejecting secret-like evidence.

### Operator readiness

- [x] governed mutation lifecycle and least-privilege operating posture documented
  in `docs/operator/v0.5-safe-administration-guide.md`;
- [x] `ExecutionUnknown` reconciliation/runbook documented; blind retry explicitly
  prohibited;
- [x] `PartiallyApplied` / `AppliedUnverified` handling documented;
- [x] destructive/CRITICAL target review and independent-approval workflow
  documented;
- [x] SQLite standalone versus PostgreSQL HA posture documented;
- [x] backup/restore implications for mutation state, claims and material-digest
  secret documented;
- [x] v0.4-to-v0.5 rollout, persistence/provider migration, in-flight operation
  handling and rollback documented in `docs/operator/v0.5-upgrade-guide.md`;
- [x] release-readiness authority is explicitly separated from release/tag/OCI/
  publication authority.

## Final exact-head admission gate

The evidence matrix above is assembled, but W40 is **not merge-ready** until the
stable final source head satisfies all of the following again:

- [ ] final exact-head `quality-gate` SUCCESS and strict up-to-date base satisfied;
- [ ] final exact-head `dependency-review` SUCCESS;
- [ ] final exact-head CodeQL C# + JavaScript/TypeScript SUCCESS;
- [ ] final exact-head GHAS aggregate reports no new alerts;
- [ ] final exact-head `kafka-smoke` SUCCESS;
- [ ] final exact-head Kafka compatibility lanes green where triggered/applicable;
- [ ] final exact-head `v05-persistence` SUCCESS;
- [ ] final exact-head v0.3/v0.4 regression benchmarks green where triggered/applicable;
- [ ] zero unresolved substantive review threads;
- [ ] fresh CODEOWNER approval from `akhiabanchian` on the stable final source head;
- [ ] current protected `main`, mergeability and repository ruleset reconciled at
  merge time.

Any source change made to address CI/review findings restarts the applicable
exact-head evidence and CODEOWNER approval requirements.

## Release decision

**Release authority remains NOT GRANTED.**

After governed W40 merge, stop for explicit owner approval before creating a v0.5
tag, promoting OCI images, publishing a GitHub Release, or taking any equivalent
publication action.