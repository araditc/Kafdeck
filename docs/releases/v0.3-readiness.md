# Kafdeck v0.3 Release Readiness

- **Release:** v0.3 — Safe Data Explorer + Server-Side Masking
- **Status:** IN PROGRESS — W24 technical readiness; release decision not yet approved
- **Scope authority:** Gate 3 / Issue #75
- **Readiness tracker:** Issue #88
- **Readiness PR:** PR #92

## 1. Release boundary

v0.3 introduces bounded, separately authorized Kafka record inspection under the released v0.2 identity/RBAC boundary.

Release invariants:

- no Kafka mutation,
- no produce/replay,
- no consumer-offset mutation,
- no payload persistence/indexing by default,
- no unbounded whole-topic scan,
- no arbitrary server-side JavaScript,
- no general-purpose SQL stream engine,
- no masking bypass,
- metadata permissions do not imply `record.read`,
- `record.export` is separate,
- server-side masking fails closed,
- Schema Registry is read-only decoding support only.

## 2. Workstream status

- W18 — Record-read contracts, authorization & budgets — merged.
- W19 — Bounded Kafka record reader — technically complete; protected-main admission is subject to repository review rules.
- W20 — Schema-assisted decoding adapter — technically complete; protected-main admission is subject to repository review rules.
- W21 — Filter/search/live-tail engine — technically complete; protected-main admission is subject to repository review rules.
- W22 — Server-side masking & bounded export — technically complete on `e2cf0be3e9eae89ea3f24c37968fc45c6654ce20`; all applicable exact-head workflows green and substantive review threads resolved.
- W23 — REST/OpenAPI + operator UI — technically complete on `4f13f96d52768c02f441192d1070f1aa6390abb5`; all applicable exact-head workflows green and substantive review threads resolved.
- W24 — Security, integration, benchmark and readiness — active in PR #92.

This record does not claim that W19+ have been admitted to protected `main`. The remote CODEOWNER rule remains authoritative for actual `main` admission.

## 3. Security/readiness package

W24 includes:

- `docs/security/v0.3-w24-threat-model-reconciliation.md`,
- `docs/operator/v0.3-record-access-masking-guide.md`,
- `docs/operator/v0.3-upgrade-guide.md`,
- `docs/testing/v0.3-readiness-evidence.md`,
- `docs/performance/v0.3-benchmark-methodology.md`,
- `tests/Kafdeck.Architecture.Tests/V03BenchmarkEvidenceTests.cs`,
- `.github/workflows/v03-benchmark.yml`,
- this release-readiness record.

## 4. Exact-head evidence ownership

An in-repository document cannot safely name its own final commit SHA and then remain the same source revision after that text changes. Therefore the immutable final exact-head evidence is recorded externally in the governed GitHub readiness tracker/PR after the source stabilizes.

Authoritative final evidence locations:

- Issue #88 — final exact W24 source SHA, workflow run IDs, benchmark values and readiness verdict,
- PR #92 — exact head, CI/check state and review-thread reconciliation,
- GitHub Actions — immutable workflow/job logs for that exact SHA.

Any source-head change invalidates earlier exact-head readiness evidence and requires fresh applicable checks.

## 5. Kafka compatibility matrix

Required final exact-head results:

| Kafka | Tier | Requirement |
| --- | --- | --- |
| 4.3.1 | Tier 1 | exact-head `kafka-compatibility` success |
| 4.2.1 | Tier 1 | exact-head `kafka-compatibility` success |
| 4.1.2 | Tier 1 | exact-head `kafka-compatibility` success |
| 3.9.2 | Tier 2 | exact-head `kafka-compatibility` success |

A transient failure is not hidden. It must be diagnosed and either repaired or followed by a successful rerun on the same exact source head before the matrix is marked ready.

W23 evidence established all four versions green on `4f13f96d52768c02f441192d1070f1aa6390abb5`; the first 3.9.2 attempt was transiently unavailable and passed on same-head rerun without source change or waiver. W24 still requires a fresh final-head matrix.

## 6. Benchmark evidence

The W24 workflow measures two bounded in-process scenarios:

- compiled structured filter evaluation,
- structured server-side masking/projection.

Initial W24 benchmark baseline on source `69ae71bfa113b3531277ade389c8890577891e72`, workflow run `35543598668`, Ubuntu 24.04 hosted runner, .NET runtime 10.0.12, four visible processors:

- **structured_filter:** 20,000 operations; 129.567 ms; **154,359.70 ops/s**,
- **structured_masking:** 20,000 operations; 297.774 ms; **67,164.96 ops/s**.

These measurements are bounded in-process release evidence only. They are not Kafka end-to-end throughput, a universal hardware claim, or a public SLA.

The final W24 head runs the same benchmark again; its exact measured values and run ID are recorded in Issue #88 after source stabilization.

## 7. Required readiness assertions

Before W24 is marked technically complete, evidence must prove:

- record authorization separation and exact target scope,
- no producer/offset-commit/mutation path,
- bounded navigation and live tail,
- full accepted Kafka matrix,
- Avro/Protobuf/JSON Schema decode and failure behavior,
- cross-cluster isolation,
- cancellation/budget/concurrency enforcement,
- fail-closed masking,
- raw/hex/export cannot bypass masking,
- payload-free successful access audit,
- no payload/secret leakage through error/audit/log/telemetry contracts,
- bounded export ownership with explicit `Indeterminate` uncertainty,
- operator/upgrade/threat-model documentation complete,
- final exact-head benchmark workflow successful.

## 8. Release decision gate

Even after every W24 technical gate is green, v0.3 must not be tagged or published automatically.

The final transition is:

```text
W24 TECHNICALLY READY
  -> exact release source identified
  -> owner reviews readiness evidence
  -> explicit v0.3 release decision
  -> only then governed release delta / tag / publication
```

**Current release authorization: NOT YET GRANTED.**